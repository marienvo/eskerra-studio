/**
 * Max time to wait for `podcastPlayerMachine` playlist persist (debounce + R2/network)
 * when draining before clearing the playlist (dismiss) or window close.
 */
export const PLAYBACK_PERSIST_DRAIN_TIMEOUT_MS = 3000;
